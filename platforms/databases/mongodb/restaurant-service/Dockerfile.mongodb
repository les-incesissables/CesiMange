FROM mongo:latest

# Copie tout le contenu de seed/ (fichiers .json et init.sh) dans /docker-entrypoint-initdb.d/
COPY seed/ /docker-entrypoint-initdb.d/

# Expose le port si nécessaire
EXPOSE 27017

# L'image officielle de Mongo exécute automatiquement tout script .sh ou .js
# présent dans /docker-entrypoint-initdb.d/ lors du premier démarrage,
# si /data/db est vide. On n'override pas ENTRYPOINT ni CMD pour s'appuyer sur le comportement par défaut.
